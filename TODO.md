输入法开发计划

1.除了pc目前另一个布局是否有用

2.请重构json系统，目录结构:语言/pc_layout.json,mobile_layout.json/config.json

3.为了以后多语言引入语言切换器，语言名称是其文件夹中config.json定义的，可以点击语言设置专门改变一个语言的设置。比如该语言是否开启pc布局

4.为了cjk输入支持引入engine抽象层，以后要实现一个中文的(现在先不)，在语言config.json可以设置所用的引擎和dict文件

5.真正实现用户体验优化，ripple效果，手势翻页动画等。material you也弄上。

6.检查bug并修复

7.实现工具栏，和候选栏共用一个栏，在没有候选词时显示文本编辑，剪切板，更多(点击进入设置)。文本编辑应该有上下左右，选择和全选，复制，粘贴等，以及退格。

8.给符号页面加入第二页测试翻页功能

9.清除所有占位符实现改为真正的。除了librime集成，中文输入暂时不添加。其他所有在代码中定义的功能都应该真实可用并且可以释放release
